import reducer from './reducers';

import * as {{ camelName }}Selectors from './selectors';
import * as {{ camelName }}Actions from './actions';
import * as {{ camelName }}Types from './types';

export {
    {{ camelName }}Selectors,
    {{ camelName }}Actions,
    {{ camelName }}Types
}

export default reducer;