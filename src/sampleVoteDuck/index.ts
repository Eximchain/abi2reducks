import reducer from './reducers';

import * as voteSelectors from './selectors';
import * as voteActions from './actions';
import * as voteTypes from './types';

export {
    voteSelectors,
    voteActions,
    voteTypes
}

export default reducer;