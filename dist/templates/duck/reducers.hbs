import { methodReducer } from '../../reusable/reducers';
import { {{ titleName }}Method } from './types';

export const {{ titleName }}Reducer = methodReducer({{ titleName }}Method);

export default {{ titleName }}Reducer;